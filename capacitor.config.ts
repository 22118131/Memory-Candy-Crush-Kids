import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.braincandyworld.kids',
  appName: 'Brain Candy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
